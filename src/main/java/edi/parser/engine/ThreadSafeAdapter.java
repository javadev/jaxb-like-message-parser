package edi.parser.engine;

/**
 * Adapters which implements this interface constructed only once.
 * Parses with slow constructor have to implements this interface.
 */
public interface ThreadSafeAdapter {
}
